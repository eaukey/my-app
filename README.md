# Frontend

Le front actif est dans `my-app/my-app` (Next app router, auth maison).

Local:
- `cd my-app/my-app`
- `npm install`
- `npm run dev`

Le dossier `my-app/` (pages router) est un ancien front minimal.
